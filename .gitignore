# Local tooling / state — NOT part of the extension
ruvector.db
.claude/
.remember/

# Dependencies & build artifacts
node_modules/
*.zip

# Store marketing images — reproducible via store-assets/_render.js & _promo.js
store-assets/*.png

# OS / editor
.DS_Store
Thumbs.db
*.log
perplexity-tts-*.zip
